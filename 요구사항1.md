# 요구사항1 — 배너만 16:5 통일 (index.html 제외)

## 수정 범위 (절대 준수)
- videos.html
- shorts.html
- places.html
- total_navi.html
- style.css / index.html 포함해서 다른 파일은 수정 금지

## 목표
- 위 4개 페이지에서 "배너(상단 hero/banner/header)" 영역만 16:5 비율로 보이게 수정한다.
- 배너 이미지가 잘리지 않고(전체가 보이게) 자연스럽게 표시되도록 한다.
- 배너 외의 모든 UI/레이아웃/콘텐츠/JS는 절대 변경하지 않는다.

## 구현 규칙
1) 각 페이지에서 배너를 담당하는 <header ...> 또는 상단 배너 wrapper를 찾는다.
2) 해당 요소의 비율/높이만 변경한다.
   - 기존에 아래 같은 형태가 있으면 16:5로 교체:
     - aspect-[16/9]  -> aspect-[16/5]
     - h-[320px] / h-[350px] / md:h-[480px] 처럼 고정높이가 있으면 제거하거나,
       max-h 방식으로 바꿔 16:5가 유지되도록 한다.
3) 배너 이미지가 "전체가 보이도록" 표시되어야 한다.
   - object-fit을 이미 contain으로 쓰고 있으면 유지(변경 금지)
   - object-fit이 cover여서 잘리면 contain으로 바꿔야 하는데,
     이 변경은 "배너 관련 CSS/클래스에서만" 최소 변경으로 한다.
4) 배너 외의 클래스/구조는 손대지 않는다.
5) 저장 후, 변경 diff를 보여준다.

## 완료 조건
- Live Server에서 videos.html 배너가 16:5로 줄어들고 이미지가 잘리지 않는다.
- shorts.html / places.html / total_navi.html도 같은 방식으로 배너가 일관되게 보인다.
