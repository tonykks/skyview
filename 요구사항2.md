# 작업: index.html 배너를 다른 페이지(videos.html)와 동일하게 통일

## 수정 범위
- Edit ONLY: index.html
- Read/Reference allowed: videos.html (읽기만)
- Do NOT edit: videos.html, style.css, 다른 모든 파일

## 목표
1) index.html의 상단 배너(= hero header)를 videos.html의 배너와 "동일한 방식"으로 만든다.
   - 동일한 배너 높이/비율(aspect 또는 height 규칙)
   - 동일한 이미지 표시 방식(object-fit 등)
   - 동일한 overlay/텍스트 위치 규칙
2) 배너 아래 영역(네비 버튼바 포함하여 아래의 intro/section/video grid 등)은
   - HTML 구조/클래스/스타일/내용을 절대 변경하지 말 것
   - 즉, index.html에서 "배너 블록"만 수정한다.

## 구현 지침
- index.html에서 배너 블록은 slider-track 또는 'Tony's Sky View' 텍스트가 있는 header/hero 영역이다.
- videos.html에서 배너에 해당하는 header/hero 블록을 찾아,
  그 구조와 핵심 클래스(폭/높이/비율/이미지 fit/overlay)를 index.html 배너에 반영한다.
- 결과는: index.html에서 videos.html과 같은 배너가 보여야 한다.

## 완료 조건
- 저장 완료
- 변경 diff는 "index.html 배너 부분"만 보여준다.
- 만약 배너 외 다른 영역이 변경되었다면 즉시 원복하고 배너만 다시 수정한다.
